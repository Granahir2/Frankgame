{
    "tasks": [
        {
            "type": "shell",
            "label": "g++ build active file",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "${fileDirname}/display.cpp",
                "${fileDirname}/easing.cpp",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "-lsfml-graphics", "-lsfml-window", "-lsfml-system"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}
